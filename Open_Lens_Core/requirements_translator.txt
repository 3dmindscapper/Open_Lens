# Core image/document handling
Pillow>=9.0.0
numpy>=1.21.0
opencv-python>=4.5.0

# OCR — EasyOCR (recommended: PyTorch-based, 80+ languages, tight bboxes)
easyocr>=1.7.0
torch>=2.0.0

# OCR — Tesseract (fallback; requires Tesseract binary – see README)
pytesseract>=0.3.10

# OCR — PaddleOCR (alternative: better on some document types)
# Uncomment or install separately.  Falls back to Tesseract if unavailable.
# paddlepaddle>=2.5.0      # CPU version; use paddlepaddle-gpu for CUDA
# paddleocr>=2.7.0

# Layout detection — Layout Parser (requires Detectron2)
# Uncomment or install separately.  Falls back to PaddleOCR or none.
# layoutparser>=0.3.4
# detectron2               # follow https://detectron2.readthedocs.io/en/latest/tutorials/install.html

# Inpainting — LaMa (deep learning, recommended for complex backgrounds)
# Uncomment or install separately.  Falls back to OpenCV TELEA.
# simple-lama-inpainting>=0.1.0

# Language detection (fully offline, statistical)
langdetect>=1.0.9

# Translation – fully local / offline neural MT
# Models (~100 MB each) are downloaded once on first use, then cached on disk.
argostranslate>=1.9.0

# PDF loading (requires Poppler binaries – see README)
pdf2image>=1.16.0

# Web server (used by web_app.py to host the translator for other devices)
flask>=3.0.0

# Optional: correct RTL text rendering (Arabic, Hebrew, Persian, Urdu)
# arabic-reshaper>=3.0.0
# python-bidi>=0.4.2
